# FlowScope 環境變數範本
#
# 用法:複製本檔為 .env,填入實際值。.env 已列入 .gitignore,不會進版控。
#     copy .env.example .env
#
# 注意:token 絕不可寫入 configs/*.yaml。
#      SPEC §9.1 的 config_snapshot 會把整份 config 明文寫進每次執行的
#      manifest.json,寫在 config 裡等於每跑一次就外洩一份。

# FinMind API token。至 https://finmindtrade.com 註冊並訂閱後取得。
# Phase 1 需要 Backer / Sponsor 層,詳見 docs/FinMind_API_Inventory.md
FINMIND_TOKEN=
